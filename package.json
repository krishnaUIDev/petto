{
  "name": "petto",
  "private": true,
  "scripts": {
    "dev:web": "pnpm -F @petto/web dev",
    "dev:desktop": "pnpm -F @petto/desktop dev",
    "dev:tauri": "pnpm -F @petto/desktop tauri dev",
    "build:web": "pnpm -F @petto/web build",
    "build:desktop": "pnpm -F @petto/desktop build",
    "build:dmg": "pnpm -F @petto/desktop tauri build"
  }
}
